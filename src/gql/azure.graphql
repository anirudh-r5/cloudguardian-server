type AzureUser {
    id: ID!
    displayName: String
    userPrincipalName: String
    mail: String
    roles: [AzureRole]!
}
type AzureRole {
    roleName: String!
    scope: String!
}

extend type Query {
    azureUsers: [AzureUser]!
}
